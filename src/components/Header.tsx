import '../sass/components/header.component.scss';

const Header = () => {
    return (
        <header>
            <nav>
                <h1>Logo</h1>
                <i className="fa fa-sign-out"></i>
            </nav>
        </header>
    );
};

export default Header;